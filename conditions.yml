- name: conditions
  hosts: all
  become: yes
  tasks:
    - name: print veggies
      ansible.builtin.debug:
        msg: fruits and veggies
      when: type == "veg"

    - name: print nonveg
      ansible.builtin.debug:
        msg: all meat
      when: type == "nonveg"


